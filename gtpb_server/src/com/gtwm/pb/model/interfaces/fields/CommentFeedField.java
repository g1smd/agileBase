package com.gtwm.pb.model.interfaces.fields;

/**
 * A placeholder field that doesn't store any value but allows a comment feed to
 * be attached, via the normal comments mechanism
 */
public interface CommentFeedField extends BaseField {

}
