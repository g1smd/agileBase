#if(!$field.getFieldCategory())
  #set($field = $sessionData.getCustomField("comment"))
#end
#foreach($comment in $view.getComments($field))
  <div class="comment">
    <span class="comment_text" #if(($foreach.count % 2) == 1) style="text-align: right" #end>$comment.getText()</span> - $comment.getAuthor(), $comment.getTimestampString()
  </div>
#end
