#if(!$field.getFieldCategory())
  #set($field = $sessionData.getCustomField("comment"))
#end
#foreach($comment in $view.getComments($field))
  <div class="comment">
    #set($t = $foreach.count % 2)
    <span class="comment_text" style="background-color: red" #if(true) style="text-align: right;" #end>$comment.getText()</span> - $comment.getAuthor(), $comment.getTimestampString()
  </div>
#end
